---
import IconLink from "./IconLink.astro";
import "./Footer.css";

import MailIcon from "../icons/mail.svg";
import GitHubIcon from "../icons/github.svg";
import LinkedInIcon from "../icons/linkedin.svg";
import ScholarIcon from "../icons/graduation-cap.svg";

import { CONTACT_ARIA_LABELS } from "../constants/aria";
import { SITE } from "../constants/site";
---

<footer class="footer">
  <div class="footer-links">
    <IconLink
      href={`mailto:${SITE.email}`}
      ariaLabel={CONTACT_ARIA_LABELS.email}
      icon={MailIcon}
      external={false}
    />

    <IconLink
      href={SITE.github}
      ariaLabel={CONTACT_ARIA_LABELS.github}
      icon={GitHubIcon}
    />

    <IconLink
      href={SITE.scholar}
      ariaLabel={CONTACT_ARIA_LABELS.scholar}
      icon={ScholarIcon}
    />

    <IconLink
      href={SITE.linkedin}
      ariaLabel={CONTACT_ARIA_LABELS.linkedin}
      icon={LinkedInIcon}
    />
  </div>

  <p class="footer-meta">
    Last updated: January 2026 / Asim Waheed
  </p>
</footer>
